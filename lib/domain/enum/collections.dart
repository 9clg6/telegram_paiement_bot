/// Collections enum
enum Collections {
  /// The users collection  
  users('users');

  /// Constructor
  /// @param id the id
  /// 
  const Collections(this.id);

  /// The id
  final String id;
}
